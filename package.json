{
  "name": "project-3-task-manager",
  "version": "1.0.0",
  "description": "MERN stack single-page application for task management",
  "main": "server/server.js",
  "Important for MERN Setup": {
    "line-10": "This package.json controls the entire application setup from the top level.",
    "line-21": "The concurrently tool runs both the client and server processes simultaneously for development."
  },
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "install": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "seed": "cd server && npm run seed"
  },
  "keywords": ["MERN", "GraphQL", "Task Manager", "MongoDB"],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}